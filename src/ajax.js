// import axios with configuration
import axios from "./axios";

// axios.get("/articles").then(({data}) => console.log(data.data));

// axios.post("/articles", {
//     title: "It's react week 10",
//     tags: ["ajax" ,"react"],
// }).then(({data}) => console.log(data.data.id));

// axios.get("/articles/22").then(({data}) => console.log(data.data));

// Add a new tag to one of your articles and console.log() the tags array from the response:

// axios.patch("/articles/62", {
//     tags: ["abc" ,"def"],
// }).then(({data}) => console.log(data.data.tags));



// Delete an article and console.log() "Deleted" once you get back a response

// axios.delete("/articles/62").then(({data}) => console.log("Deleted"));

// Add a comment to an article using axios and `console.log()` its ID

// axios.post("/articles/22/comments", {
//     email: "abc@abc.com",
//     comment: "this and that",
// }).then(({data}) => console.log(data.data.id));

// Use console.log() to list all of the comments for one of your articles

// axios.get("/articles/22/comments").then(({data}) => console.log(data.data));

// Use console.log() to output a list of all the tags

// axios.get("/tags").then(({data}) => console.log(data.data));